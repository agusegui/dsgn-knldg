Plan a layout composition using the workflow in `skills/design-principles/modes/compose.md`.

Check `skills/design-principles/local/layout.md` for committed compositional preferences (signature compositions, anti-patterns specific to the brand) before falling back to `core/layout/`.
