Suggest font pairings using the workflow in `skills/design-principles/modes/type-pair.md`.

Before generating candidates, check `skills/design-principles/local/type.md` — if a pairing is already committed there, the user is asking either to confirm it, propose alternatives, or audit it. Confirm intent before running the 3-candidate process.

Otherwise run the full process from `core/typography/` knowledge.
