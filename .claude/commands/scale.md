Derive a type scale and spacing system using the workflow in `skills/design-principles/modes/scale.md`.

Check `skills/design-principles/local/type.md` for a committed scale before deriving a new one. If found, use the committed ratio and base size.
