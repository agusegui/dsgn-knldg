Derive a brand-track color palette using the workflow in `skills/design-principles/modes/palette.md`.

Check `skills/design-principles/local/palette.md` first — if a palette is committed, use it instead of generating a new one. The user may be asking to confirm, audit, or extend it.

For product/software/design-system work, defer to `/oklch-skill` (Jakub Krehel) — see `skills/design-principles/core/color/oklch.md` for the routing rules.
