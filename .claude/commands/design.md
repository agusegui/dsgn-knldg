Run the full design workflow defined in `skills/design-principles/modes/design.md`. Read that file and follow every step.

Before any decision, check `skills/design-principles/local/` for committed brand decisions (palette, type, voice, components, layout, overview). If a relevant local file exists, use it instead of generating from scratch — `local/` always wins over `core/`. Fall back to `skills/design-principles/core/` knowledge only when no local override exists.
