Render a font specimen card on the Paper canvas using the workflow in `skills/design-principles/modes/type-specimen.md`.

Use the pairing committed in `skills/design-principles/local/type.md` if present and the user hasn't specified other fonts.
