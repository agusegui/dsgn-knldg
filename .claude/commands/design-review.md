Run the design review workflow defined in `skills/design-principles/modes/design-review.md`. Read that file and follow it.

When checking a design, evaluate against `skills/design-principles/core/` knowledge AND any committed `skills/design-principles/local/` decisions. A design that violates a local commitment (e.g. uses a font outside `local/type.md`) is a finding, not just a stylistic divergence.
